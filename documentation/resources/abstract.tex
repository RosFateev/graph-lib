\textbf{graph-lib} is a C++ graph library currently aimed at implementation of algorithms used to solve network flow problem: for a directed graph with given edge capacities find maximum possible to send flow from source vertex to sink vertex.